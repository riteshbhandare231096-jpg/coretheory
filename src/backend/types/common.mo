/// Cross-cutting types shared across all domains.
module {
  /// Nanosecond timestamp (from Time.now())
  public type Timestamp = Int;

  /// Canister principal used as a user identifier
  public type UserId = Principal;
};
